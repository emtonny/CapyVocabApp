// Custom 3D Dialog Overlay (xác nhận thoát / đăng xuất)
// TODO: Sinh bởi scaffold tự động từ FRD/Use Case. Cần hiện thực hoá chi tiết.

import 'package:flutter/material.dart';

/// Widget dùng lại cho Custom 3D Dialog Overlay (xác nhận thoát / đăng xuất)
class CustomConfirmDialog extends StatelessWidget {
  const CustomConfirmDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // TODO: implement UI
  }
}
