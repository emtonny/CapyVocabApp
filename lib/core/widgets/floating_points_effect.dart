// spawnFloatingPoints() hiệu ứng +N xu nổi lên
// TODO: Sinh bởi scaffold tự động từ FRD/Use Case. Cần hiện thực hoá chi tiết.

import 'package:flutter/material.dart';

/// Widget dùng lại cho spawnFloatingPoints() hiệu ứng +N xu nổi lên
class FloatingPointsEffect extends StatelessWidget {
  const FloatingPointsEffect({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // TODO: implement UI
  }
}
