// UC-AUTH-01: Tab Đăng nhập / Đăng ký + Social OAuth
// TODO: Sinh bởi scaffold tự động từ FRD/Use Case. Cần hiện thực hoá chi tiết.

import 'package:flutter/material.dart';

/// UI screen tương ứng UC-AUTH-01: Tab Đăng nhập / Đăng ký + Social OAuth
class AuthScreen extends StatelessWidget {
  const AuthScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AuthScreen'),
      ),
    );
  }
}
