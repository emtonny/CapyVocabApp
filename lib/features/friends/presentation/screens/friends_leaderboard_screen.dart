// FR-FRND-01: xếp hạng tuần + danh sách bạn bè
// TODO: Sinh bởi scaffold tự động từ FRD/Use Case. Cần hiện thực hoá chi tiết.

import 'package:flutter/material.dart';

/// UI screen tương ứng FR-FRND-01: xếp hạng tuần + danh sách bạn bè
class FriendsLeaderboardScreen extends StatelessWidget {
  const FriendsLeaderboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('FriendsLeaderboardScreen'),
      ),
    );
  }
}
