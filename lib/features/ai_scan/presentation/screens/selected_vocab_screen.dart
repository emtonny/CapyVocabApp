// UC-STOR-01: giỏ từ vựng đã chọn (Batch Extract)
// TODO: Sinh bởi scaffold tự động từ FRD/Use Case. Cần hiện thực hoá chi tiết.

import 'package:flutter/material.dart';

import '../../../../shared/widgets/graph_paper_background.dart';

/// UI screen tương ứng UC-STOR-01: giỏ từ vựng đã chọn (Batch Extract)
class SelectedVocabScreen extends StatelessWidget {
  const SelectedVocabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const GraphPaperScaffold(
      body: Center(
        child: Text('SelectedVocabScreen'),
      ),
    );
  }
}
