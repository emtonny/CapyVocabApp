// UC-CHAT-01: danh sách hội thoại + tìm kiếm
// TODO: Sinh bởi scaffold tự động từ FRD/Use Case. Cần hiện thực hoá chi tiết.

import 'package:flutter/material.dart';

/// Widget dùng lại cho UC-CHAT-01: danh sách hội thoại + tìm kiếm
class ChatInboxView extends StatelessWidget {
  const ChatInboxView({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // TODO: implement UI
  }
}
