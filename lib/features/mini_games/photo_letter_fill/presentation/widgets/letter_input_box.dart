// FR-GAME2-02: shakeEmpty + viền cam khi rỗng
// TODO: Sinh bởi scaffold tự động từ FRD/Use Case. Cần hiện thực hoá chi tiết.

import 'package:flutter/material.dart';

/// Widget dùng lại cho FR-GAME2-02: shakeEmpty + viền cam khi rỗng
class LetterInputBox extends StatelessWidget {
  const LetterInputBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // TODO: implement UI
  }
}
