// FR-GAME1-01: sắp xếp từ theo ảnh
// TODO: Sinh bởi scaffold tự động từ FRD/Use Case. Cần hiện thực hoá chi tiết.

import 'package:flutter/material.dart';

/// UI screen tương ứng FR-GAME1-01: sắp xếp từ theo ảnh
class PhotoOrderQuizScreen extends StatelessWidget {
  const PhotoOrderQuizScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PhotoOrderQuizScreen'),
      ),
    );
  }
}
