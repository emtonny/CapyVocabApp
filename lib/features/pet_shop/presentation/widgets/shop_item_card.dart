// FR-SHOP-01: giá tiền / trang bị / đang dùng
// TODO: Sinh bởi scaffold tự động từ FRD/Use Case. Cần hiện thực hoá chi tiết.

import 'package:flutter/material.dart';

/// Widget dùng lại cho FR-SHOP-01: giá tiền / trang bị / đang dùng
class ShopItemCard extends StatelessWidget {
  const ShopItemCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // TODO: implement UI
  }
}
